export type signUpInputs = {
  fullName: string;
  email: string;
  password: string;
  rePassword: string;
  phoneNumber: string;
  country: string;
};

export type codeInput = {
  code: string;
};

export type idCardInput = {
    idCard: FileList;
};